<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>Subspace Music</title>
  </head>
  <body>
    <header>
      <h1>Subspace Music</h1>
    </header>
    <div id="root">
      <main>
        <form action="/<%= category %>s/delete/<%= id %>" method="post">
          <h2>Delete <%= category %></h2>
          <p>Are you sure?</p>
          <div class="delete-actions">
            <button type="submit" class="confirm-delete">Yes</button>
            <a href="/<%= category %>s/">
              <button type="button" class="no-delete">No</button>
            </a>
          </div>
        </form>
      </main>
    </div>
  </body>
</html>
